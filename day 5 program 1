#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    // Check if n is a power of 2
    if (n > 0 && (n & (n - 1)) == 0)
        printf("yes");
    else
        printf("no");

    return 0;
}
